---
description: Mandatory change verification before task completion
alwaysApply: true
---

# Backend Change Validation

- Before finishing a task, select checks based on changed areas rather than skipping validation.
- Minimum for TypeScript changes: `pnpm lint:typescript`.
- For docs/rules-only changes, run formatting/checks that apply to changed Markdown/JSON/shell files.
- For route, middleware, MQ, DB, env, or infra changes, choose from this matrix:
  - TypeScript/backend code: `pnpm lint:fix`, `pnpm lint:typescript`.
  - Env config: `pnpm validate:env` when a representative `.env` is available.
  - Build/runtime entrypoints: `pnpm build`.
  - Route/API contract: `pnpm lint:typescript`, targeted tests if available, and OpenAPI metadata inspection.
  - DB schema: run `pnpm lint:typescript` when schema files changed; run `pnpm db:generate` when schema files changed and include migration artifacts. Do not run `pnpm db:migrate`, `pnpm db:seed`, or `pnpm db:studio` unless the owner explicitly asks for that exact command in the current task.
  - External API codegen: relevant `api:download:*`, `pnpm api:generate`, then TypeScript check.
  - Tests changed or behavior covered by tests: `pnpm test:unit` or targeted Vitest; `pnpm test` when broader validation is needed.
- If full checks were not run, state it explicitly and list what remains unverified.
- Do not claim full completion without actual validation or explicit constraints.
- For DB schema changes, include generated migration files from `pnpm db:generate` in the same change. Do not run `pnpm db:migrate` from an agent workflow.

## Quality Principle

- Rules define policy and standards.
- Skills define step-by-step workflows.
- `AGENTS.md` contains the shared validation contract; keep this rule aligned with it.
