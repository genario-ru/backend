/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/socialkit/client";
import fetch from "@/lib/socialkit/client";

import type {
  GetTiktokTranscript400,
  GetTiktokTranscript403,
  GetTiktokTranscript404,
  GetTiktokTranscript500,
  GetTiktokTranscriptQueryParams,
  GetTiktokTranscriptQueryResponse,
} from "../models/get-tiktok-transcript.ts";

function getGetTiktokTranscriptUrl() {
  const res = { method: "GET", url: `/tiktok/transcript` as const };
  return res;
}

/**
 * @description Extract the full transcript (with timestamps) from a TikTok video.
 * @summary Tiktok Transcript
 * {@link /tiktok/transcript}
 */
export async function getTiktokTranscript(
  { params }: { params?: GetTiktokTranscriptQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTiktokTranscriptQueryResponse,
    ResponseErrorConfig<
      | GetTiktokTranscript400
      | GetTiktokTranscript403
      | GetTiktokTranscript404
      | GetTiktokTranscript500
    >,
    unknown
  >({
    method: "GET",
    url: getGetTiktokTranscriptUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
