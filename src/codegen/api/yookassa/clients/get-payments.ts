/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/yookassa/client";
import fetch from "@/lib/yookassa/client";

import type {
  GetPayments400,
  GetPayments401,
  GetPayments403,
  GetPayments500,
  GetPaymentsQueryParams,
  GetPaymentsQueryResponse,
} from "../models/get-payments.ts";
import { getPaymentsQueryResponseSchema } from "../zod/get-payments-schema.ts";

function getGetPaymentsUrl() {
  const res = { method: "GET", url: `/payments` as const };
  return res;
}

/**
 * @description Use this request to get a list of payments. You can download payments created over the last 3 years. You can filter the list by specified criteria. More about working with lists: https://yookassa.ru/developers/using-api/lists
 * @summary List payments
 * {@link /payments}
 */
export async function getPayments(
  { params }: { params?: GetPaymentsQueryParams } = {},
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetPaymentsQueryResponse,
    ResponseErrorConfig<
      GetPayments400 | GetPayments401 | GetPayments403 | GetPayments500
    >,
    unknown
  >({
    method: "GET",
    url: getGetPaymentsUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return getPaymentsQueryResponseSchema.parse(res.data);
}
