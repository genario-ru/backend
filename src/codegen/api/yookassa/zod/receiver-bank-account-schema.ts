/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "@/lib/zod/index.ts";

import { receiverSchema } from "./receiver-schema.ts";

/**
 * @description Реквизиты для пополнения банковского счета.
 */
export const receiverBankAccountSchema = z
  .lazy(() => receiverSchema)
  .and(
    z.object({
      type: z.literal("bank_account"),
    }),
  )
  .and(
    z.object({
      account_number: z
        .string()
        .max(20)
        .regex(/\d{20}/)
        .describe("Номер банковского счета. Формат — 20 символов."),
      bic: z
        .string()
        .max(9)
        .regex(/\d{9}/)
        .describe(
          "Банковский идентификационный код (БИК) банка, в котором открыт счет. Формат — 9 символов.",
        ),
      type: z.enum(["bank_account"]),
    }),
  )
  .describe("Реквизиты для пополнения банковского счета.");
