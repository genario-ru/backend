/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "@/lib/zod/index.ts";

import { clientIpSchema } from "./client-ip-schema.ts";
import { paymentMethodsConfirmationDataRedirectSchema } from "./payment-methods-confirmation-data-redirect-schema.ts";
import { recipientSchema } from "./recipient-schema.ts";

/**
 * @description Данные для проверки и сохранения способа оплаты.
 */
export const savePaymentMethodDataSchema = z
  .object({
    type: z.enum(["bank_card"]),
    get holder() {
      return recipientSchema
        .describe(
          "Получатель платежа. Нужен, если вы разделяете потоки платежей в рамках одного аккаунта или создаете платеж в адрес другого аккаунта.",
        )
        .optional();
    },
    get client_ip() {
      return clientIpSchema
        .describe(
          "IPv4 или IPv6-адрес пользователя. Если не указан, используется IP-адрес TCP-подключения.",
        )
        .optional();
    },
    get confirmation() {
      return paymentMethodsConfirmationDataRedirectSchema.optional();
    },
  })
  .describe("Данные для проверки и сохранения способа оплаты.");
