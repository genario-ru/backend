/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "@/lib/zod/index.ts";

import { errorBodySchema } from "./error-body-schema.ts";
import { videoListPageSchema } from "./video-list-page-schema.ts";

export const getVideosByAuthorIdPathParamsSchema = z.object({
  author_id: z
    .string()
    .regex(/^[0-9]+$/)
    .describe("Числовой id автора."),
});

export const getVideosByAuthorIdQueryParamsSchema = z.object({
  page: z.coerce
    .number()
    .int()
    .min(1)
    .default(1)
    .describe("Номер страницы (с 1)."),
  limit: z.coerce
    .number()
    .int()
    .min(1)
    .max(100)
    .default(20)
    .describe("Количество элементов на странице."),
});

/**
 * @description Успешный ответ
 */
export const getVideosByAuthorId200Schema = z.lazy(() => videoListPageSchema);

/**
 * @description Ресурс не найден или ошибка клиента. Тело может быть JSON с `detail` или HTML-страница.
 */
export const getVideosByAuthorId404Schema = z
  .lazy(() => errorBodySchema)
  .describe("Типичный JSON при 404 и части других ошибок API.");

/**
 * @description Ошибка сервера. Тело может быть JSON с `detail` или HTML-страница.
 */
export const getVideosByAuthorId500Schema = z
  .lazy(() => errorBodySchema)
  .describe("Типичный JSON при 404 и части других ошибок API.");

export const getVideosByAuthorIdQueryResponseSchema = z.lazy(
  () => getVideosByAuthorId200Schema,
);
