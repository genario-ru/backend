/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/rutube/client";
import fetch from "@/lib/rutube/client";

import type {
  GetVideosByAuthorId404,
  GetVideosByAuthorId500,
  GetVideosByAuthorIdPathParams,
  GetVideosByAuthorIdQueryParams,
  GetVideosByAuthorIdQueryResponse,
} from "../models/get-videos-by-author-id.ts";
import { getVideosByAuthorIdQueryResponseSchema } from "../zod/get-videos-by-author-id-schema.ts";

function getGetVideosByAuthorIdUrl({
  author_id,
}: {
  author_id: GetVideosByAuthorIdPathParams["author_id"];
}) {
  const res = {
    method: "GET",
    url: `/api/video/person/${author_id}/` as const,
  };
  return res;
}

/**
 * @description Постраничный список видео автора. Пагинация: `page`, размер страницы — query-параметр `limit` (в ответе отражается как `per_page`).
 * @summary Список видео канала
 * {@link /api/video/person/:author_id/}
 */
export async function getVideosByAuthorId(
  {
    author_id,
    params,
  }: {
    author_id: GetVideosByAuthorIdPathParams["author_id"];
    params?: GetVideosByAuthorIdQueryParams;
  },
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetVideosByAuthorIdQueryResponse,
    ResponseErrorConfig<GetVideosByAuthorId404 | GetVideosByAuthorId500>,
    unknown
  >({
    method: "GET",
    url: getGetVideosByAuthorIdUrl({ author_id }).url.toString(),
    params,
    ...requestConfig,
  });
  return getVideosByAuthorIdQueryResponseSchema.parse(res.data);
}
